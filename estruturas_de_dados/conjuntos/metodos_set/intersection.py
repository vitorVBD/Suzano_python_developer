# .intersection() - Retorna um conjunto que é a interseção (valores pertencentes aos 2 conjuntos) de dois conjuntos.

conjunto_a = {1, 2, 3, 4, 5}
conjunto_b = {4, 5, 6, 7, 8}

intersecao = conjunto_a.intersection(conjunto_b)
print(intersecao)  # {4, 5}